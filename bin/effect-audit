#!/usr/bin/env bash
# effect-audit — wrapper that execs the bun script from this plugin.
# Auto-PATHed by Claude Code while the plugin is enabled.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
exec bun run "$SCRIPT_DIR/../scripts/effect-audit.ts" "$@"
